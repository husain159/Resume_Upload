# Resume_Upload
A RESTful API built with Django and Django REST Framework that allows users to securely upload, manage, and retrieve resumes. The API supports multiple file formats (PDF, DOCX) and integrates authentication for secure access. This project demonstrates backend file handling, CRUD operations, and API development using Django.
